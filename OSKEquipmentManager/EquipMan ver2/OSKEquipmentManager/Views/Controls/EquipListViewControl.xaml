<UserControl
    x:Class="OSKEquipmentManager.Views.Controls.EquipListViewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OSKEquipmentManager.Views.Controls"
    xmlns:views="using:OSKEquipmentManager.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding UpdateFormViewModel, Source={StaticResource Locator}}"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <StackPanel>
                <TextBox x:Name="Search" VerticalAlignment="Top" Height="20" Width="200"/>
                <Button Content="＋" x:Name="AddButton" Height="20" Click="AddButton_Click"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Frame>
                <views:EquipmentListPage/>
            </Frame>
        </Grid>
    </Grid>
</UserControl>
